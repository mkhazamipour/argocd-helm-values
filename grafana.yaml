serviceMonitor:
  enabled: false
persistence:
  type: pvc
  enabled: true
  accessModes:
    - ReadWriteOnce
  size: 10Gi
admin:
  existingSecret: "grafana-admin-password"
  userKey: admin-user
  passwordKey: admin-password
ingress:
  enabled: false
deploymentStrategy:
  type: Recreate
