controller:
  watchIngressWithoutClass: true
  hostNetwork: true
  hostPort:
    enabled: true
  kind: Deployment
  service:
    type: NodePort
  metrics:
    enabled: false
    serviceMonitor:
      enabled: true
  admissionWebhooks:
    enabled: false
  nodeSelector:
    router: enabled
  tolerations:
   - key: "key"
     operator: "Exists"
     value: "value"
     effect: "NoSchedule"
  updateStrategy:
    rollingUpdate:
    type: Recreate
defaultBackend:
  enabled: true
